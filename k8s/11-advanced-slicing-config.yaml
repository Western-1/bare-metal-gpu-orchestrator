apiVersion: v1
kind: ConfigMap
metadata:
  name: nvidia-device-plugin-config-advanced
  namespace: kube-system
data:
  config.yaml: |
    version: v1
    sharing:
      timeSlicing:
        resources:
        - name: nvidia.com/gpu
          replicas: 7
